import sys
from astropy.io import fits


def test(base, mask):
    base_fits = fits.open(base)
    mask_fits = fits.open(mask)
    print(base_fits.info())
    print(mask_fits.info())


if __name__ == '__main__':
    test(sys.argv[1], sys.argv[2])
    pass
